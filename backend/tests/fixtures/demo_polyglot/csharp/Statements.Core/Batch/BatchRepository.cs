namespace Statements.Core.Batch
{
    /// <summary>Reads and writes batch records.</summary>
    public class BatchRepository
    {
        /// <summary>Fetches a batch by id.</summary>
        public Batch Get(long id)
        {
            return new Batch();
        }
    }
}
