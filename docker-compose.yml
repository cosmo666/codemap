name: codemap

services:
  backend:
    build: ./backend
    env_file:
      - path: backend/.env
        required: false
    ports: ["8000:8000"]
    volumes:
      - ${CODEMAP_REPOS:-./}:/repos
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
